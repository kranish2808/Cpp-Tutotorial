#include<iostream>
using namespace std;

int main()
{
    
  /*
    AND - &  (true if both value is 1 else false)
    OR - | (true if any value is 1 & false for both both zero value )
    NOT - ~ (true if value is 0 & false is value is 1)
    XOR - ^ (true if only one value is 1 & false for both one and both zero )
             */
   int a =3;
   int b=6;

   /*  cout <<"a&b" << a&b ;
      cout <<"a|b" << a|b ;
      cout <<"a~b" << ~a ;
      cout <<"a^b" << a^b ;
*/
    cout <<"a~b" << ~a ;
}